# Task Completion Checklist

1. Run `mix format` to ensure code formatting
2. Run `mix test` to verify all tests pass
3. Run `mix review` for full quality gate (compile warnings, credo, sobelow, dialyzer)
